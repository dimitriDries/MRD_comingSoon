# Images checklist

Use this list to track which assets you need. Recommended formats: WebP or PNG/JPEG; optimize for web (compressed).

---

- [ ] **MonsieurData logo**  
  **Where:** Header (and optionally footer)  
  **Path:** `/public/images/MonsieurData_logo.png`  
  **Specs:** Works at display height ~2rem (32px); should read clearly on white/light background. SVG or PNG with transparency preferred.  
  **Notes:** Already referenced in code; replace or keep as-is.

- [ ] **Hero visual**  
  **Where:** Hero section (main landing block)  
  **Specs:** Landscape; recommend **1200×800** (or 600×400 min) for retina. Aspect ratio ~3:2.  
  **Notes:** Replaces current placeholder. Use an image that supports your value proposition (e.g. data, dashboards, analytics).

- [ ] **About section image**  
  **Where:** About teaser (SectionAbout), right column  
  **Specs:** **500×350** or similar ratio (~10:7).  
  **Notes:** Photo, illustration, or diagram that fits “who you are” / “what you do”.

- [ ] **Client logo 1**  
  **Where:** Section “Trusted by…” (SectionClientLogos)  
  **Specs:** Aspect ratio **2:1** (e.g. 200×100 or 240×120). Grayscale applied in UI.  
  **Notes:** PNG/SVG with transparent or white background.

- [ ] **Client logo 2**  
  **Where:** SectionClientLogos  
  **Specs:** 2:1 ratio; e.g. 200×100 or 240×120. Grayscale in UI.

- [ ] **Client logo 3**  
  **Where:** SectionClientLogos  
  **Specs:** 2:1 ratio; grayscale in UI.

- [ ] **Client logo 4**  
  **Where:** SectionClientLogos  
  **Specs:** 2:1 ratio; grayscale in UI.

- [ ] **Client logo 5**  
  **Where:** SectionClientLogos  
  **Specs:** 2:1 ratio; grayscale in UI.

- [ ] **Client logo 6**  
  **Where:** SectionClientLogos  
  **Specs:** 2:1 ratio; grayscale in UI.

- [ ] **Favicon**  
  **Where:** Browser tab, bookmarks  
  **Specs:** **32×32** (and optionally 16×16). ICO or PNG.  
  **Notes:** Add to `index.html` / `public` when ready.

- [ ] **OG image (social share)**  
  **Where:** When the site is shared on social (e.g. LinkedIn, Twitter)  
  **Specs:** **1200×630** px.  
  **Notes:** Optional; add via `<meta property="og:image" content="…">` when you have a share URL.
